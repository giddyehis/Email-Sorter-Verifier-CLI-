# Email-Sorter-Verifier-CLI-
Interactive tool that sorts email addresses by their provider/MX records into separate files and folders and verifies email addresses and splits them into "clean" (valid) and "bounced" (invalid) lists.
